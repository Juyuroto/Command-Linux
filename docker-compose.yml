services:
  frontend:
    container_name: linux-command
    build:
      context: ./frontend
      dockerfile: dockerfile
    restart: unless-stopped
    ports:
      - 3005:3005
    volumes:
      - ./frontend:/app:Z
      - /app/node_modules
    environment:
      - "${FRONTEND_PORT}:5000"